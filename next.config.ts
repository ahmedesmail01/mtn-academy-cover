/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["efficaciousleadership.com", "managethenow.com"], // Add all external domains here
  },
};

module.exports = nextConfig;
